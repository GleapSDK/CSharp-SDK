<Window x:Class="GleapSDK.Sample.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gleap="clr-namespace:GleapSDK.WebView2;assembly=Gleap.WebView2"
        Title="Gleap WebView2 Sample" Height="720" Width="1080">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <!-- Left: a mock host application with the Gleap launcher floating on top of it. -->
        <Grid Grid.Column="0" Background="#F4F5F7">
            <StackPanel Margin="40" VerticalAlignment="Top">
                <TextBlock Text="Your app" FontSize="30" FontWeight="Bold" Foreground="#222" />
                <TextBlock TextWrapping="Wrap" Margin="0,10,0,0" Foreground="#555" FontSize="14"
                           Text="This area represents your application's own UI. The Gleap launcher floats in the bottom-right corner — click it (or the Open button) to slide the messenger in as an overlay, just like the JavaScript SDK. Closing the widget (its ✕) drops back to the launcher." />
            </StackPanel>

            <!-- The reusable launcher control: floating button + messenger overlay. -->
            <gleap:GleapMessenger x:Name="Messenger" />
        </Grid>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="SDK Key" FontWeight="Bold" />
            <TextBox x:Name="SdkKeyBox" Margin="0,4,0,8" />
            <Button Content="Initialize" Click="OnAttach" Margin="0,0,0,12" />

            <Button Content="Open" Click="OnOpen" Margin="0,2" />
            <Button Content="Close" Click="OnClose" Margin="0,2" />
            <Button Content="Start Conversation" Click="OnStartConversation" Margin="0,2" />
            <Button Content="Start Bot" Click="OnStartBot" Margin="0,2" />
            <Button Content="Open Help Center" Click="OnOpenHelpCenter" Margin="0,2" />
            <Button Content="Open News" Click="OnOpenNews" Margin="0,2" />
            <Button Content="Track Event" Click="OnTrackEvent" Margin="0,2" />
            <Button Content="Set Custom Data" Click="OnSetCustomData" Margin="0,2" />

            <TextBlock x:Name="StatusText" TextWrapping="Wrap" Margin="0,12,0,0" Foreground="Gray" />
        </StackPanel>
    </Grid>
</Window>
